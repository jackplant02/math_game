[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mathquiz"
version = "0.1.0"
dependencies = ["numpy"]

[project.scripts]
mathquiz = "mathquiz.math_game:main"

[project.optional-dependencies]
dev = ["pytest", "pyinstaller"]